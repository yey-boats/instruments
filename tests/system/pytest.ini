[pytest]
# Run from the project root: `pytest tests/system/unattended`. The
# rootdir setting + the empty conftest path lets imports like
# `from tests.system.inject import nmea0183` resolve.
testpaths = unattended
addopts = -ra -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
