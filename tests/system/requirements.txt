pytest>=7.0
requests>=2.28
websockets>=12.0
# Injection transport. Bleak is required for BLE; pyserial only if
# ESPDISP_SERIAL_PORT is set. Both are listed so a single
# `pip install -r requirements.txt` covers either path.
bleak>=0.21
pyserial>=3.5
