; PlatformIO project for Yey Boats Instruments
; https://github.com/yey-boats/instruments
;
; SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0
;
; Envs:
;   esp32-4848s040  - production firmware build (Sunton ESP32-4848S040)
;   release-*       - CI release builds; same boards, debug/test controls off
;   ota             - extends above; sets upload_protocol=espota. Pass IP at runtime:
;                       pio run -e ota -t upload --upload-port <ip>
;   native          - host-side unit tests for the SignalK delta parser

[env:esp32-4848s040]
; Spec 21 A is currently parked - the IDF 5.x bump surfaces 36 stricter
; format-string sites + NimBLE-Arduino 1.4 incompatibility (esp_bt.h
; moved); A's realistic cost is 5-7 days, not 2-3. See spec 21 for the
; updated estimate. Keeping the Arduino 2.x + IDF 4.4 path stable while
; A is scheduled.
platform = espressif32@^6.7.0
board = esp32-s3-devkitc-1
framework = arduino

board_build.mcu = esp32s3
board_build.f_cpu = 240000000L
board_build.flash_mode = qio
board_build.flash_size = 16MB
board_build.partitions = default_16MB.csv
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_build.arduino.memory_type = qio_opi

build_flags =
    -D ARDUINO_USB_CDC_ON_BOOT=0
    -D BOARD_HAS_PSRAM
    -D LV_CONF_INCLUDE_SIMPLE
    -D LV_LVGL_H_INCLUDE_SIMPLE
    -I include
    -DCORE_DEBUG_LEVEL=3
    -D BOARD_ID_DEFINED
    -D BOARD_ID_SUNTON_4848S040
    ; Stack canaries on our own code. The prebuilt Arduino core ships with
    ; CONFIG_COMPILER_STACK_CHECK_MODE_NONE, so the sdkconfig toggle is inert;
    ; this compiler flag applies -fstack-protector to the firmware sources
    ; where the large stack temporaries live, turning silent overflow into a
    ; named __stack_chk_fail panic (captured in the prevboot ring). newlib/IDF
    ; provides __stack_chk_guard/_fail.
    -fstack-protector-strong

monitor_speed = 115200
upload_speed = 115200
extra_scripts =
    pre:tools/version.py
    post:tools/merge_factory.py

lib_deps =
    moononournation/GFX Library for Arduino@~1.4.7
    lvgl/lvgl@^9.2.0
    https://github.com/TAMCTec/gt911-arduino.git
    h2zero/NimBLE-Arduino@^1.4.2
    links2004/WebSockets@^2.4.1
    bblanchon/ArduinoJson@^7.2.0

[env:esp32-4848s040-arduino]
extends = env:esp32-4848s040
platform = espressif32@^6.7.0
framework = arduino

[env:ota]
extends = env:esp32-4848s040
upload_protocol = espota
upload_flags =
    --port=3232

; Lab-only debug build. Same firmware as the production env plus opt-in
; debug features compiled in via YEYBOATS_DEBUG_* flags. Never ship this
; to end users:
;   - YEYBOATS_DEBUG_UDP_LOG: broadcasts every logf() line to UDP
;     <broadcast>:UDP_LOG_PORT for the lab listener
;     (tools/lab-logger/) to capture and rotate.
;   - -fstack-protector-strong: turns silent stack overflow into a clean
;     panic with backtrace, which the crash-forensics RTC ring (commit
;     1bc8ddb) then re-emits as [prevboot] lines on the next boot. The
;     pre-fix `handle_logs` 19 KiB stack array sat undetected for who
;     knows how long because canaries were off in sdkconfig.
;     `-strong` covers functions with arrays/refs/structs - the realistic
;     hit list for embedded code - without the runtime cost of `-all`.
; Add new debug flags here; production env stays minimal and stable.
[env:esp32-4848s040-debug]
extends = env:esp32-4848s040
build_flags =
    ${env:esp32-4848s040.build_flags}
    -D YEYBOATS_DEBUG_UDP_LOG=1
    -fstack-protector-strong

; OTA variant of the debug build for flashing the lab device without USB.
[env:ota-debug]
extends = env:esp32-4848s040-debug
upload_protocol = espota
upload_flags =
    --port=3232

; Performance-counter build: enables the bench-sweep harness + all DBG_PERF
; counters (render/CPU/throughput/network/memory) via -DDBG_PERF_COUNTERS.
; Lab/bench only; the counters are compiled out of every other env.
[env:esp32-4848s040-perf]
extends = env:esp32-4848s040
build_flags =
    ${env:esp32-4848s040.build_flags}
    -D DBG_PERF_COUNTERS=1
    -D YEYBOATS_DEBUG_UDP_LOG=1
    -fstack-protector-strong

; OTA variant of the perf build.
[env:ota-perf]
extends = env:esp32-4848s040-perf
upload_protocol = espota
upload_flags =
    --port=3232

; --- esp-idf 5.x migration spike (spec 21) ------------------------------------
; pioarduino platform = matched Arduino-ESP32 3.x + IDF 5.1 (the official
; espressif32 platform stalls at Arduino 2.x). Unlocks esp_lcd RGB num_fbs=2
; (double-buffer / vsync flip) for flicker-free DIRECT rendering.
;
; BLE is disabled (-DYEYBOATS_DISABLE_BLE) for this slice: NimBLE-Arduino 1.4
; doesn't build on IDF 5; re-enable later with NimBLE-Arduino 2.x (works on the
; Arduino-3.x API this platform ships). lib_deps therefore drops NimBLE.
;
; KNOWN-INCOMPLETE: Arduino_GFX's Arduino_RGB_Display is implemented only in its
; ESP_ARDUINO_VERSION_MAJOR<3 branch, so this env will not link until the
; display is moved fully onto esp_lcd (spec 21 H: esp_lcd_panel_io_3wire_spi for
; the ST7701 init + esp_lcd_new_rgb_panel with num_fbs=2). Tracking env only.
[env:esp32-4848s040-idf5]
extends = env:esp32-4848s040-perf
platform = https://github.com/pioarduino/platform-espressif32/releases/download/53.03.13/platform-espressif32.zip
; HYBRID (arduino + espidf): rebuilds IDF from source so sdkconfig.defaults
; applies (IRAM-safe esp_lcd -> bounce buffers work -> RGB cache-panic fix +
; tear-free double-buffer; BT/NimBLE on). custom_component_remove is the
; pioarduino mechanism that DELETES these deps from arduino's idf_component.yml
; so the component manager never fetches them (EXCLUDE_COMPONENTS only excludes
; post-fetch, so esp_rainmaker's https_server.crt embed still ran and hit a
; PlatformIO doubled-path bug). We use none of the RainMaker/IoT/speech/zigbee
; stack. Keep mdns/qrcode/libsodium/littlefs.
framework = arduino, espidf
custom_component_remove =
    espressif/esp_rainmaker
    espressif/rmaker_common
    espressif/esp_insights
    espressif/esp_diagnostics
    espressif/esp-sr
    espressif/esp-zboss-lib
    espressif/esp-zigbee-lib
    espressif/esp-dsp
    espressif/esp-modbus
    espressif/esp_modem
    espressif/network_provisioning
    chmorgan/esp-libhelix-mp3
build_flags =
    ${env:esp32-4848s040.build_flags}
    -D DBG_PERF_COUNTERS=1
    -D YEYBOATS_DEBUG_UDP_LOG=1
    -D RENDER_DOUBLE_BUFFER=1
    -fstack-protector-strong
lib_deps =
    moononournation/GFX Library for Arduino@~1.4.7
    lvgl/lvgl@^9.2.0
    https://github.com/TAMCTec/gt911-arduino.git
    h2zero/NimBLE-Arduino@^2.2.0
    links2004/WebSockets@^2.4.1
    bblanchon/ArduinoJson@^7.2.0

; Spec 13 step 10: second supported board.
; Waveshare ESP32-S3-Touch-LCD Tier 1 profiles. Display init still lives in
; main.cpp until spec 13 step 3 lands; these differ at build time by board id,
; geometry, layout class, capabilities, NMEA2000/CAN, and backlight pin.
[env:waveshare-touch-lcd-4]
extends = env:esp32-4848s040
build_flags =
    -D ARDUINO_USB_CDC_ON_BOOT=0
    -D BOARD_HAS_PSRAM
    -D LV_CONF_INCLUDE_SIMPLE
    -D LV_LVGL_H_INCLUDE_SIMPLE
    -I include
    -DCORE_DEBUG_LEVEL=3
    -D BOARD_ID_DEFINED
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_4
    -D ENABLE_NMEA2000
    -D TOUCH_INT=16
    -D TOUCH_INT_ACTIVE_LOW=1

[env:waveshare-touch-lcd-4_3]
extends = env:esp32-4848s040
build_flags =
    ${env:waveshare-touch-lcd-4.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_4_3
    -D LCD_W=800
    -D LCD_H=480

[env:waveshare-touch-lcd-4_3b]
extends = env:esp32-4848s040
build_flags =
    ${env:waveshare-touch-lcd-4.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_4_3B
    -D LCD_W=800
    -D LCD_H=480

[env:waveshare-touch-lcd-5_800x480]
extends = env:esp32-4848s040
build_flags =
    ${env:waveshare-touch-lcd-4.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_5_800X480
    -D LCD_W=800
    -D LCD_H=480

[env:waveshare-touch-lcd-5_1024x600]
extends = env:esp32-4848s040
build_flags =
    ${env:waveshare-touch-lcd-4.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_5_1024X600
    -D LCD_W=1024
    -D LCD_H=600

[env:waveshare-touch-lcd-7_800x480]
extends = env:esp32-4848s040
build_flags =
    ${env:waveshare-touch-lcd-4.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_7_800X480
    -D LCD_W=800
    -D LCD_H=480

[env:waveshare-touch-lcd-7b_1024x600]
extends = env:esp32-4848s040
build_flags =
    ${env:waveshare-touch-lcd-4.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_7B_1024X600
    -D LCD_W=1024
    -D LCD_H=600

; Waveshare ESP32-S3-Knob-Touch-LCD-1.8 (round 360x360 ST77916 QSPI + encoder).
[env:waveshare-knob-1_8]
extends = env:esp32-4848s040
build_flags =
    -D ARDUINO_USB_CDC_ON_BOOT=1
    -D BOARD_HAS_PSRAM
    -D LV_CONF_INCLUDE_SIMPLE
    -D LV_LVGL_H_INCLUDE_SIMPLE
    -I include
    -DCORE_DEBUG_LEVEL=3
    -D BOARD_ID_DEFINED
    -D BOARD_ID_WAVESHARE_KNOB_1_8
    -D LCD_W=360
    -D LCD_H=360
    -fstack-protector-strong
lib_deps =
    ${env:esp32-4848s040.lib_deps}
    madhephaestus/ESP32Encoder@^0.11.7
    mathertel/OneButton@^2.6.1
    fbiego/CST816S@^1.1.1

[release_common]
build_flags =
    -D ARDUINO_USB_CDC_ON_BOOT=0
    -D BOARD_HAS_PSRAM
    -D LV_CONF_INCLUDE_SIMPLE
    -D LV_LVGL_H_INCLUDE_SIMPLE
    -I include
    -DCORE_DEBUG_LEVEL=0
    -D BOARD_ID_DEFINED
    -D YEYBOATS_RELEASE_BUILD=1
    -D YEYBOATS_ENABLE_INPUT_TEST=0
    -D YEYBOATS_ENABLE_BENCH=0
    -D YEYBOATS_ENABLE_DEMO=0
    -D YEYBOATS_ENABLE_STALL_TELEMETRY=0
    -D YEYBOATS_ENABLE_TOUCH_CAL_UI=0

[release_waveshare_common]
build_flags =
    ${release_common.build_flags}
    -D ENABLE_NMEA2000
    -D TOUCH_INT=16
    -D TOUCH_INT_ACTIVE_LOW=1

[env:release-esp32-4848s040]
extends = env:esp32-4848s040
build_flags =
    ${release_common.build_flags}
    -D BOARD_ID_SUNTON_4848S040

[env:release-waveshare-touch-lcd-4]
extends = env:waveshare-touch-lcd-4
build_flags =
    ${release_waveshare_common.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_4

[env:release-waveshare-touch-lcd-4_3]
extends = env:waveshare-touch-lcd-4_3
build_flags =
    ${release_waveshare_common.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_4_3
    -D LCD_W=800
    -D LCD_H=480

[env:release-waveshare-touch-lcd-4_3b]
extends = env:waveshare-touch-lcd-4_3b
build_flags =
    ${release_waveshare_common.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_4_3B
    -D LCD_W=800
    -D LCD_H=480

[env:release-waveshare-touch-lcd-5_800x480]
extends = env:waveshare-touch-lcd-5_800x480
build_flags =
    ${release_waveshare_common.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_5_800X480
    -D LCD_W=800
    -D LCD_H=480

[env:release-waveshare-touch-lcd-5_1024x600]
extends = env:waveshare-touch-lcd-5_1024x600
build_flags =
    ${release_waveshare_common.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_5_1024X600
    -D LCD_W=1024
    -D LCD_H=600

[env:release-waveshare-touch-lcd-7_800x480]
extends = env:waveshare-touch-lcd-7_800x480
build_flags =
    ${release_waveshare_common.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_7_800X480
    -D LCD_W=800
    -D LCD_H=480

[env:release-waveshare-touch-lcd-7b_1024x600]
extends = env:waveshare-touch-lcd-7b_1024x600
build_flags =
    ${release_waveshare_common.build_flags}
    -D BOARD_ID_WAVESHARE_TOUCH_LCD_7B_1024X600
    -D LCD_W=1024
    -D LCD_H=600

[env:release-waveshare-knob-1_8]
extends = env:waveshare-knob-1_8
build_flags =
    ${release_common.build_flags}
    -D BOARD_ID_WAVESHARE_KNOB_1_8
    -D LCD_W=360
    -D LCD_H=360
lib_deps =
    ${env:esp32-4848s040.lib_deps}
    madhephaestus/ESP32Encoder@^0.11.7
    mathertel/OneButton@^2.6.1
    fbiego/CST816S@^1.1.1

; Headless protocol test harness on a bare ESP32-S3-DevKitC-1. No display,
; no LVGL/touch. It now sits on the SAME base infrastructure as the main
; firmware: net::setup()/net::loop() bring up the WiFi manager, ArduinoOTA
; (<deviceId>.local:3232), BLE NUS + CONNECTION, mDNS, and the dispatchCommand
; console funnel. That makes the harness OTA-updatable and reconfigurable over
; BLE/serial without a reflash.
;
; build_src_filter approach: include the full non-UI source set (+<*>) then
; EXCLUDE every TU that pulls in LVGL / Arduino_GFX (main.cpp, src/ui/*, the
; display/touch/screenshot/web/knob-LVGL TUs). harness_main.cpp + harness_stubs.cpp
; replace them; harness_stubs.cpp no-ops the handful of ui:: / knob_ui:: /
; manager_screens:: symbols the linked infra references. lib_deps therefore needs
; the full infra set (ArduinoJson, WebSockets, NimBLE) but NOT lvgl / GFX.
[env:harness-s3-devkitc]
platform = espressif32@^6.7.0
board = esp32-s3-devkitc-1
framework = arduino
board_build.mcu = esp32s3
board_build.flash_size = 16MB
board_build.partitions = default_16MB.csv
; DevKitC-1 N16R8 = octal PSRAM; the qio_opi memory_type makes the early-boot
; PSRAM cache config match the octal part. Without it the app faults at entry
; (deterministic reset before any console output). Matches the base board env.
board_build.flash_mode = qio
board_build.arduino.memory_type = qio_opi
build_flags =
    -D ARDUINO_USB_CDC_ON_BOOT=1
    -D BOARD_HAS_PSRAM
    -I include
    ; Headless-only <lvgl.h> shim so infra headers (ui_screens.h / ui_theme.h /
    ; widget_registry.h) parse without linking LVGL. See harness_shim/lvgl.h.
    -I harness_shim
    -DCORE_DEBUG_LEVEL=3
    -D YEYBOATS_HARNESS=1
build_src_filter =
    +<*>
    -<main.cpp>
    -<ui/>
    -<fonts/>
    -<font_resolver_lvgl.cpp>
    -<knob_input.cpp>
    -<lvgl_alloc.cpp>
    -<layout_renderer.cpp>
    -<manager_screens.cpp>
    -<midl_render_apply.cpp>
    -<screenshot.cpp>
    -<touch_cal.cpp>
    -<web.cpp>
    -<widget_registry.cpp>
    +<harness_main.cpp>
    +<harness_stubs.cpp>
lib_deps =
    bblanchon/ArduinoJson@^7.2.0
    links2004/WebSockets@^2.4.1
    h2zero/NimBLE-Arduino@^1.4.2
monitor_speed = 115200
extra_scripts = pre:tools/version.py

; BLE-central variant of the harness: same headless DevKitC build on the same
; base infra, but loop() runs the on-demand NimBLE central soak (scan -> connect
; -> attach -> switch -> detach -> disconnect) instead of the IP cycle.
; -D HARNESS_BLE selects it. Build with: pio run -e harness-s3-devkitc-ble
[env:harness-s3-devkitc-ble]
extends = env:harness-s3-devkitc
build_flags =
    -D ARDUINO_USB_CDC_ON_BOOT=1
    -D BOARD_HAS_PSRAM
    -I include
    -I harness_shim
    -DCORE_DEBUG_LEVEL=3
    -D YEYBOATS_HARNESS=1
    -D HARNESS_BLE=1

; OTA variants of the harness: flash the headless DevKitC over WiFi like the
; main firmware (ArduinoOTA is wired by net::setup at <deviceId>.local:3232).
;   pio run -e ota-harness-s3-devkitc -t upload --upload-port <harness-ip>
;   (or: make ota ENV=ota-harness-s3-devkitc DEVICE_IP=<harness-ip>)
[env:ota-harness-s3-devkitc]
extends = env:harness-s3-devkitc
upload_protocol = espota
upload_flags =
    --port=3232

[env:ota-harness-s3-devkitc-ble]
extends = env:harness-s3-devkitc-ble
upload_protocol = espota
upload_flags =
    --port=3232

[env:native]
platform = native
test_framework = unity
test_build_src = yes
build_src_filter = -<*> +<signalk_parser.cpp> +<layout.cpp> +<boat_data.cpp> +<nmea0183_parser.cpp> +<autopilot_pure.cpp> +<manager_config.cpp> +<font_resolver.cpp> +<widget_data_resolver.cpp> +<boards/board_common.cpp> +<boards/board_native_fake.cpp> +<ui/layout_context.cpp> +<manager_url.cpp> +<manager_endpoint.cpp> +<error_log.cpp> +<hostname_check.cpp> +<ui_config_check.cpp> +<sources_check.cpp> +<log_level_check.cpp> +<device_discovery.cpp> +<net_health.cpp> +<knob_menu.cpp> +<proto/proto.cpp> +<path_store.cpp> +<bench_row.cpp> +<capabilities.cpp> +<subscription_set.cpp> +<config_persist.cpp> +<marker_math.cpp> +<value_format.cpp> +<midl_solve.cpp> +<metric_source_map.cpp> +<metric_value.cpp> +<midl_render.cpp>
build_flags =
    -std=c++17
    -I include
    -DUNITY_INCLUDE_DOUBLE
    -DUNITY_DOUBLE_PRECISION=1e-12
    -DARDUINOJSON_USE_DOUBLE=1
    -DBOARD_ID_DEFINED
    -DBOARD_ID_NATIVE_FAKE
lib_deps =
    bblanchon/ArduinoJson@^7.2.0
test_filter =
    test_parser
    test_layout
    test_boat_data
    test_nmea0183
    test_autopilot
    test_manager_config
    test_font_resolver
    test_widget_data
    test_board
    test_ota_pass
    test_manager_url
    test_manager_endpoint
    test_error_log
    test_hostname_check
    test_ui_config_check
    test_sources_check
    test_log_level_check
    test_device_discovery
    test_knob_menu
    test_proto
    test_path_store
    test_bench_row
    test_capabilities
    test_subscription_set
    test_config_persist
    test_zoom
    test_marker_math
    test_value_format
    test_metric_value
    test_midl_projection
    test_midl_solve
    test_midl_render
    test_midl_select

; ---------------------------------------------------------------------------
; Resolution test harness (host, headless LVGL). Renders the real ui_layouts
; code at a build-defined resolution and snapshots a BMP. Run per display
; class via tools/sim_render.sh (pass -DSIM_LCD_W/-DSIM_LCD_H). No SDL needed.
[env:sim]
platform = native
lib_deps =
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.2.0
build_flags =
    -std=gnu++17
    -DARDUINO=10805
    ; ARDUINO is defined so autopilot.h's API is declared, but keep ArduinoJson
    ; off the Arduino (Stream/Print/Flash) path so it stays host-clean.
    -DARDUINOJSON_ENABLE_ARDUINO_STRING=0
    -DARDUINOJSON_ENABLE_ARDUINO_STREAM=0
    -DARDUINOJSON_ENABLE_ARDUINO_PRINT=0
    -DARDUINOJSON_ENABLE_PROGMEM=0
    -DLV_CONF_INCLUDE_SIMPLE
    -I sim
    -I include
    -include sim/esp_heap_caps.h
build_src_filter =
    -<*>
    +<ui/ui_layouts.cpp>
    +<metric_value.cpp>
    +<ui/ui_markers.cpp>
    +<marker_math.cpp>
    +<ui/ui_theme.cpp>
    +<layout.cpp>
    +<subscription_set.cpp>
    +<autopilot_pure.cpp>
    +<value_format.cpp>
    +<fonts/font_xl_64.c>
    +<../sim/sim_main.cpp>
    +<../sim/stubs.cpp>

; Same headless host harness as [env:sim], but renders the baked YB-MIDL demo
; document (include/midl_demo_doc.h: SQUARE_480_JSON) through the REAL device
; render path — midl::solve_screen -> midl::render::map_element ->
; ui::layouts::create_freeform — mirroring src/midl_render_apply.cpp's apply_doc()
; flow (which is device-only). The snapshot is a design-parity preview of the
; firmware's MIDL render; compare to preview-square-480.png.
;   pio run -e sim-midl && .pio/build/sim-midl/program docs/sim-shots/midl-square-480.bmp
[env:sim-midl]
platform = native
lib_deps =
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.2.0
build_flags =
    -std=gnu++17
    -DARDUINO=10805
    -DARDUINOJSON_ENABLE_ARDUINO_STRING=0
    -DARDUINOJSON_ENABLE_ARDUINO_STREAM=0
    -DARDUINOJSON_ENABLE_ARDUINO_PRINT=0
    -DARDUINOJSON_ENABLE_PROGMEM=0
    -DLV_CONF_INCLUDE_SIMPLE
    -I sim
    -I include
    -include sim/esp_heap_caps.h
build_src_filter =
    -<*>
    +<ui/ui_layouts.cpp>
    +<metric_value.cpp>
    +<ui/ui_markers.cpp>
    +<marker_math.cpp>
    +<ui/ui_theme.cpp>
    +<layout.cpp>
    +<subscription_set.cpp>
    +<autopilot_pure.cpp>
    +<midl_solve.cpp>
    +<midl_render.cpp>
    +<metric_source_map.cpp>
    +<value_format.cpp>
    +<fonts/font_xl_64.c>
    +<../sim/sim_midl.cpp>
    +<../sim/stubs.cpp>

; Same headless host harness as [env:sim], but renders the fullscreen wind dial
; (src/ui/screen_wind.cpp) instead of the dashboard tiles, so the A / T wind
; indices and the current arrow can be eyeballed without a panel.
;   pio run -e sim-wind && .pio/build/sim-wind/program docs/sim-shots/wind.bmp
[env:sim-wind]
platform = native
lib_deps =
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.2.0
build_flags =
    -std=gnu++17
    -DARDUINO=10805
    -DARDUINOJSON_ENABLE_ARDUINO_STRING=0
    -DARDUINOJSON_ENABLE_ARDUINO_STREAM=0
    -DARDUINOJSON_ENABLE_ARDUINO_PRINT=0
    -DARDUINOJSON_ENABLE_PROGMEM=0
    -DLV_CONF_INCLUDE_SIMPLE
    -I sim
    -I include
    -include sim/esp_heap_caps.h
build_src_filter =
    -<*>
    +<ui/screen_wind.cpp>
    +<ui/ui_compass.cpp>
    +<ui/ui_theme.cpp>
    +<fonts/font_xl_64.c>
    +<../sim/sim_wind.cpp>
    +<../sim/stubs.cpp>

; Same headless host harness, but renders the REAL autopilot HUD
; (src/ui/screen_autopilot.cpp) — the semicircular compass, target bug, XTE
; strip, and numeric tiles — so the reference redesign can be eyeballed at every
; display class without a panel.
;   pio run -e sim-ap && .pio/build/sim-ap/program docs/sim-shots/ap.bmp
[env:sim-ap]
platform = native
lib_deps =
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.2.0
build_flags =
    -std=gnu++17
    -DARDUINO=10805
    -DARDUINOJSON_ENABLE_ARDUINO_STRING=0
    -DARDUINOJSON_ENABLE_ARDUINO_STREAM=0
    -DARDUINOJSON_ENABLE_ARDUINO_PRINT=0
    -DARDUINOJSON_ENABLE_PROGMEM=0
    -DLV_CONF_INCLUDE_SIMPLE
    -I sim
    -I include
    -include sim/esp_heap_caps.h
build_src_filter =
    -<*>
    +<ui/screen_autopilot.cpp>
    +<ui/ui_compass.cpp>
    +<ui/ui_markers.cpp>
    +<marker_math.cpp>
    +<ui/ui_theme.cpp>
    +<value_format.cpp>
    +<fonts/font_xl_64.c>
    +<../sim/sim_ap.cpp>
    +<../sim/stubs.cpp>

; Same headless host harness, but renders the QuadGrid-template screens (nav,
; depth, steering, route, trip) and the full-screen "zoom" view, so every screen
; can be eyeballed at every display class. Screen id is argv[1].
;   pio run -e sim-screens && .pio/build/sim-screens/program nav out.bmp
[env:sim-screens]
platform = native
lib_deps =
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.2.0
build_flags =
    -std=gnu++17
    -DARDUINO=10805
    -DARDUINOJSON_ENABLE_ARDUINO_STRING=0
    -DARDUINOJSON_ENABLE_ARDUINO_STREAM=0
    -DARDUINOJSON_ENABLE_ARDUINO_PRINT=0
    -DARDUINOJSON_ENABLE_PROGMEM=0
    -DLV_CONF_INCLUDE_SIMPLE
    -I sim
    -I include
    -include sim/esp_heap_caps.h
build_src_filter =
    -<*>
    +<ui/ui_layouts.cpp>
    +<metric_value.cpp>
    +<ui/ui_markers.cpp>
    +<marker_math.cpp>
    +<ui/ui_compass.cpp>
    +<ui/ui_theme.cpp>
    +<ui/screen_nav.cpp>
    +<ui/screen_depth.cpp>
    +<ui/screen_steering.cpp>
    +<ui/screen_route.cpp>
    +<ui/screen_trip.cpp>
    +<ui/screen_wind_steer.cpp>
    +<ui/screen_wind_classic.cpp>
    +<layout.cpp>
    +<subscription_set.cpp>
    +<autopilot_pure.cpp>
    +<value_format.cpp>
    +<fonts/font_xl_64.c>
    +<../sim/sim_screens.cpp>
    +<../sim/stubs.cpp>

; Same headless host harness, but renders the Waveshare-knob round views
; (src/ui/knob_ui.cpp: ap_hud / compass / wind / big) at 360x360 with the
; board id defined so the board-gated dedicated views compile. The view is
; chosen at runtime (argv[1]); see tools/sim_render.sh for the gallery loop.
;   pio run -e sim-knob && .pio/build/sim-knob/program ap_hud docs/sim-shots/knob-ap_hud.bmp
[env:sim-knob]
platform = native
lib_deps =
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.2.0
build_flags =
    -std=gnu++17
    -DARDUINO=10805
    -DARDUINOJSON_ENABLE_ARDUINO_STRING=0
    -DARDUINOJSON_ENABLE_ARDUINO_STREAM=0
    -DARDUINOJSON_ENABLE_ARDUINO_PRINT=0
    -DARDUINOJSON_ENABLE_PROGMEM=0
    -DLV_CONF_INCLUDE_SIMPLE
    -DBOARD_ID_DEFINED
    -DBOARD_ID_WAVESHARE_KNOB_1_8
    -DSIM_LCD_W=360
    -DSIM_LCD_H=360
    -I sim
    -I include
    -include sim/esp_heap_caps.h
build_src_filter =
    -<*>
    +<ui/knob_ui.cpp>
    +<ui/ui_theme.cpp>
    +<knob_menu.cpp>
    +<fonts/font_xl_64.c>
    +<../sim/sim_knob.cpp>
    +<../sim/stubs_knob.cpp>

; Same headless host harness, but renders the REAL "controlled" frame overlay
; (src/ui/control_frame.cpp: ui::control_frame::build + set_sessions) driven by
; synthetic proto::Session structs. Shape (rect 480x480 / round 360x360) and the
; active-session count are runtime argv, so one binary covers both panels:
;   pio run -e sim-control && .pio/build/sim-control/program rect 3 out.bmp
[env:sim-control]
platform = native
lib_deps =
    lvgl/lvgl@^9.2.0
    bblanchon/ArduinoJson@^7.2.0
build_flags =
    -std=gnu++17
    -DARDUINO=10805
    -DARDUINOJSON_ENABLE_ARDUINO_STRING=0
    -DARDUINOJSON_ENABLE_ARDUINO_STREAM=0
    -DARDUINOJSON_ENABLE_ARDUINO_PRINT=0
    -DARDUINOJSON_ENABLE_PROGMEM=0
    -DLV_CONF_INCLUDE_SIMPLE
    -DSIM_LCD_W=480
    -DSIM_LCD_H=480
    -I sim
    -I include
    -include sim/esp_heap_caps.h
build_src_filter =
    -<*>
    +<ui/control_frame.cpp>
    +<ui/ui_theme.cpp>
    +<proto/proto.cpp>
    +<../sim/sim_control.cpp>
